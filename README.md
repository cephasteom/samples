# Samples

Add samples in folders. Web browsers like wavs.

* `npm i` to install dependencies
* `npm run compile` at the root of the project to compile new samples.json file, containing any new samples
* to access json file online, use https://raw.githubusercontent.com/<username>/<repo>/<branch>/samples.json
